/**
 * Require.js Configuration File
 * 
 * The first javascript file that is loaded. Set up all of the
 * required paths. 
 */

requirejs.config({
    baseUrl: "js/",
    paths: {
        jquery: 'libs/jquery',
        pulse: 'libs/pulse'
    }
});